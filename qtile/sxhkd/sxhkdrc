super + Escape
    pkill -USR1 -x sxhkd

super + o
    pcmanfm

    alt + F1
    conky

    alt + F2
    vlc

    alt + F3
    mpv

    super + alt +t
    tilix
    
   alt + F12
    bash /home/salion/powermenu/rofi-power-menu.sh
    
   alt + F11
   bash /home/salion/powermenu/rofi-monitors.sh
    
    alt + F10
   bash /home/salion/powermenu/rofi-wifi-menu.sh

#Rofi & dmenu

super + d
    rofi -show drun -show-icons -font "JetBrainsMono Nerd Font 14"

super + p
    dmenu_run

alt + z; {a,s}
   {viddesc, poddesc}

super + g; {g,k,a,o} 
  {flatpak run org.gimp.GIMP,flatpak run org.kde.kdenlive,audacity,flatpak run com.obsproject.Studio}

super + e
  kitty -e nvim

alt + r
    kitty -e lvim

super + s; c
    clipmenu

alt + g
    kitty -e /home/salion/Documentos/Telegram/micro/micro-2.0.11/micro

alt + x
 geany

alt + p
 filezilla

# Application Keybindings

super + w
  qutebrowser

 ctrl +e 
 firefox
 
ctrl + w
    midori
alt + s
 xfce4-screenshooter

alt + n 
    nitrogen

ctrl + alt + s
    flameshot

#Audio Keybindings

XF86AudioRaiseVolume
        amixer -c 1 -- sset Master 2db+

XF86AudioLowerVolume
        amixer -c 1 -- sset Master 2db-

XF86AudioMute
        amixer -c 1 -- sset Master toggle

alt + d
        amixer -c 1 -- sset Master 2db+

alt + a
        amixer -c 1 -- sset Master 2db-

XF86AudioPlay
    playerctl play-pause

ctrl + alt + p
    playerctl play-pause

XF86AudioNext
    playerctl next

XF86AudioPrev
    playerctl previous

ctrl + shift + period
    playerctl next

ctrl + shift + comma
    playerctl previous

XF86AudioStop
    playerctl stop

alt + {h,j,k,l}
   mpv {prev,next,play,pause}

